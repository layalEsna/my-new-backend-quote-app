{
  "name": "my-new-backend-quote-app",
  "version": "1.0.0",
  "description": "Backend for quote application",
  "main": "index.js",
  "scripts": {
    "start": "HOST=0.0.0.0 json-server --watch db.json --port $PORT"
  }
  ,
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
